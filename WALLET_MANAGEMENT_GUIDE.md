# 多钱包管理使用指南

## 📍 如何访问钱包管理功能

### 方法 1：通过设置页面
1. 打开应用
2. 点击底部导航栏的 **"设置"** 图标
3. 在设置页面找到 **"钱包管理"** 卡片
4. 点击 **"查看所有钱包"** 按钮

### 方法 2：直接访问
在浏览器地址栏输入：`http://localhost:5173/wallets`

---

## 🎯 功能说明

### 1️⃣ 创建新钱包

**首次创建：**
1. 访问 `/create-wallet` 或点击 "创建钱包" 按钮
2. 系统自动生成 12 个助记词
3. **重要**：抄写并妥善保管助记词
4. 验证助记词（按顺序选择单词）
5. 设置钱包密码
6. 完成创建

**创建第二个钱包：**
1. 在钱包管理页面 (`/wallets`)
2. 点击底部的 **"+ 创建新钱包"** 按钮
3. 重复上述步骤

---

### 2️⃣ 导入钱包

1. 在钱包管理页面点击 **"📥 导入钱包"**
2. 输入 12 个助记词（用空格分隔）
3. 设置钱包名称
4. 设置密码
5. 完成导入

---

### 3️⃣ 查看所有钱包

在 `/wallets` 页面可以看到：
- 所有钱包列表（卡片式展示）
- 当前活动钱包（蓝色边框高亮 + ✅ 图标）
- 每个钱包的：
  - 钱包名称
  - 各链地址预览（ETH、SOL、BTC、TRX）
  - 链数量
  - 创建时间

---

### 4️⃣ 切换钱包

1. 在钱包列表中找到要切换的钱包
2. 点击 **"切换"** 按钮
3. 自动跳转到解锁页面
4. 输入该钱包的密码
5. 解锁成功后，该钱包成为活动钱包

**注意**：切换钱包后需要重新解锁才能使用。

---

### 5️⃣ 重命名钱包

1. 点击钱包卡片上的 **"重命名"** 按钮
2. 输入新名称
3. 点击 **"确认"**
4. 重命名成功

---

### 6️⃣ 备份钱包（导出助记词）

1. 点击钱包卡片上的 **"备份"** 按钮
2. 输入钱包密码
3. 查看 12 个助记词（网格显示）
4. 点击 **"📋 复制"** 或手写记录
5. 点击 **"完成"** 关闭

**⚠️ 安全提示**：
- 助记词是恢复钱包的唯一方式
- 不要泄露给任何人
- 妥善保管，建议离线备份

---

### 7️⃣ 删除钱包

1. 点击钱包卡片上的 **"删除"** 按钮
2. 阅读安全警告
3. 输入钱包密码确认
4. 点击 **"确认删除"**

**⚠️ 重要警告**：
- 删除操作无法撤销
- 删除前请确保已备份助记词
- 删除后将永久失去访问权限

---

## 🔐 安全说明

### 密码保护
- 删除钱包需要密码
- 备份钱包需要密码
- 切换钱包需要重新解锁

### 会话管理
- 切换钱包自动锁定
- 关闭浏览器自动锁定
- 需要重新输入密码解锁

### 数据隔离
- 每个钱包独立加密
- 不同的盐值和 IV
- 无法跨钱包访问数据

---

## 📱 使用场景

### 场景 1：个人 + 工作钱包分离
```
钱包 1：个人钱包（日常使用）
钱包 2：工作钱包（项目资金）
```

### 场景 2：测试 + 主网分离
```
钱包 1：测试网钱包（开发测试）
钱包 2：主网钱包（真实资产）
```

### 场景 3：多账户管理
```
钱包 1：主账户
钱包 2：备用账户
钱包 3：交易账户
```

---

## 🎨 界面说明

### 钱包卡片
```
┌─────────────────────────────────────┐
│ ✅ 我的主钱包          [当前活动钱包] │
│                                     │
│ ETH: 0x1234...5678                  │
│ SOL: 4w2yJ6...7u2h                  │
│ BTC: 1A1zP1...3FvkU                 │
│ TRX: TGZ2Ys...dbtMt                 │
│                                     │
│ 4 条链 • 创建于 2026-02-26          │
│                                     │
│ [重命名] [备份] [删除]              │
└─────────────────────────────────────┘
```

### 活动钱包标识
- ✅ 图标
- 蓝色边框
- "当前活动钱包" 标签

---

## ⚙️ 技术细节

### 存储位置
- **localStorage**: `multichain_wallets_data`
  - 存储所有钱包的加密数据
  - 包含活动钱包 ID

- **sessionStorage**: `multichain_wallet_session`
  - 存储当前解锁的钱包 ID
  - 会话级别，关闭浏览器自动清除

### 钱包 ID 格式
```
wallet_1709020800000_abc123def
```

### 支持的链
- ✅ Ethereum (ETH)
- ✅ Solana (SOL)
- ✅ Bitcoin (BTC)
- ✅ Tron (TRX)

---

## ❓ 常见问题

### Q: 可以创建多少个钱包？
A: 理论上无限制，但建议不超过 50 个（localStorage 限制）。

### Q: 忘记密码怎么办？
A: 无法恢复密码，但可以使用助记词重新导入钱包。

### Q: 删除钱包后能恢复吗？
A: 不能。删除后只能通过助记词重新导入。

### Q: 切换钱包后原来的钱包还在吗？
A: 在。切换只是改变活动钱包，不会删除其他钱包。

### Q: 可以在不同设备使用同一个钱包吗？
A: 可以。使用助记词在其他设备导入即可。

---

## 🚨 重要提示

1. **仅用于测试目的**
   - 不适合存储真实资产
   - 建议仅在测试网使用

2. **备份助记词**
   - 这是恢复钱包的唯一方式
   - 建议离线备份
   - 不要截图或保存在云端

3. **保护密码**
   - 使用强密码
   - 不要与他人共享
   - 定期更换密码

4. **浏览器数据**
   - 清除浏览器数据会丢失钱包
   - 删除前务必备份助记词

---

## 📞 需要帮助？

如果遇到问题，请检查：
1. 浏览器控制台是否有错误
2. 是否已正确解锁钱包
3. 密码是否正确
4. 助记词是否完整

---

**祝您使用愉快！** 🎉
