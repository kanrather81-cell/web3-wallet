# 钱包导入功能测试指南

## 🚀 快速开始

开发服务器已经在运行：`http://localhost:5173`

测试入口：`http://localhost:5173/import-wallet`

## 📝 测试步骤

### 测试1：助记词导入（推荐）

#### 步骤1：访问导入页面

1. 打开浏览器访问：
   ```
   http://localhost:5173/import-wallet
   ```

2. 验证页面显示：
   - ✅ 标题："导入现有钱包"
   - ✅ 两个模式按钮：助记词导入 / 私钥导入
   - ✅ 默认选中"助记词导入"

#### 步骤2：输入助记词

使用以下测试助记词（标准 BIP39）：

```
abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about
```

或者使用你自己的测试助记词（12个单词）。

**注意：** 不要使用包含真实资产的助记词！

#### 步骤3：设置钱包信息

1. **钱包名称：** "测试导入钱包"
2. **密码：** "Test@1234"
3. **确认密码：** "Test@1234"
4. 勾选"我已理解..."复选框

#### 步骤4：导入钱包

1. 点击"导入钱包"按钮
2. 等待导入完成（约1-2秒）
3. 验证：
   - ✅ 自动跳转到主页（`/`）
   - ✅ 可以看到钱包地址
   - ✅ 显示多链资产

#### 步骤5：验证多链地址

1. 在主页查看各链的地址
2. 验证地址格式：
   - Ethereum: `0x` + 40个十六进制字符
   - Solana: Base58 编码
   - Bitcoin: 以 `1`, `3`, 或 `bc1` 开头
   - Tron: `T` + 33个字符

### 测试2：私钥导入（Ethereum）

#### 步骤1：切换到私钥导入模式

1. 访问 `http://localhost:5173/import-wallet`
2. 点击"私钥导入"按钮
3. 验证界面切换

#### 步骤2：选择链

1. 点击"Ethereum"卡片
2. 验证：
   - ✅ Ethereum 卡片高亮显示
   - ✅ 私钥输入框显示正确的提示

#### 步骤3：输入私钥

使用测试私钥（64位十六进制）：

```
0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
```

**注意：** 这是一个示例私钥，不要使用真实的私钥！

#### 步骤4：设置密码

1. **密码：** "Test@1234"
2. **确认密码：** "Test@1234"
3. 勾选安全协议

#### 步骤5：尝试导入

1. 点击"导入钱包"
2. 验证：
   - ✅ 显示功能限制提示
   - ✅ 说明私钥导入处于测试阶段

### 测试3：错误处理

#### 测试3.1：无效助记词

1. 选择"助记词导入"
2. 输入无效的助记词：
   ```
   test test test
   ```
3. 设置密码并点击"导入钱包"
4. 验证：
   - ✅ 显示错误："无效的助记词，请检查输入"

#### 测试3.2：密码不匹配

1. 输入有效的助记词
2. 密码："Test@1234"
3. 确认密码："Test@5678"（不同）
4. 点击"导入钱包"
5. 验证：
   - ✅ 显示错误："两次输入的密码不一致"

#### 测试3.3：弱密码

1. 输入有效的助记词
2. 密码："123"（太弱）
3. 点击"导入钱包"
4. 验证：
   - ✅ 显示错误："密码强度不足"
   - ✅ 显示具体的改进建议

#### 测试3.4：未勾选协议

1. 输入有效的助记词和密码
2. 不勾选"我已理解..."复选框
3. 点击"导入钱包"
4. 验证：
   - ✅ 显示错误："请阅读并同意安全提示"

### 测试4：私钥格式验证

#### 测试4.1：Ethereum 私钥

**有效格式：**
```
0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
```

**无效格式：**
```
0x123                    # 太短
xyz...                   # 非十六进制
```

#### 测试4.2：Solana 私钥

**有效格式：**
- Base58 编码
- 约 80-90 个字符

**无效格式：**
- 太短或太长的字符串

#### 测试4.3：Bitcoin 私钥

**有效格式：**
- WIF 格式
- 以 5/K/L 开头
- 50-51 个字符

**无效格式：**
- 其他格式

#### 测试4.4：Tron 私钥

**有效格式：**
```
1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
```

**无效格式：**
- 非64位十六进制

### 测试5：密码强度指示器

测试不同的密码组合：

| 密码 | 预期强度 | 颜色 |
|------|---------|------|
| `123` | 弱 | 红色 |
| `12345678` | 弱 | 红色 |
| `abcdefgh` | 弱 | 红色 |
| `Test1234` | 中 | 黄色 |
| `Test@123` | 强 | 绿色 |
| `MyWallet@2024` | 强 | 绿色 |

### 测试6：UI/UX 测试

#### 测试6.1：模式切换

1. 点击"助记词导入"
2. 验证：显示助记词输入框
3. 点击"私钥导入"
4. 验证：显示链选择器和私钥输入框
5. 再次点击"助记词导入"
6. 验证：切换回助记词界面

#### 测试6.2：链选择器

1. 选择"私钥导入"模式
2. 依次点击每个链：
   - Ethereum
   - Solana
   - Bitcoin
   - Tron
3. 验证：
   - ✅ 选中的链高亮显示
   - ✅ 私钥输入框提示更新
   - ✅ 格式说明更新

#### 测试6.3：响应式设计

1. 调整浏览器窗口大小
2. 测试不同屏幕尺寸：
   - 桌面（1920x1080）
   - 平板（768x1024）
   - 手机（375x667）
3. 验证：
   - ✅ 布局自适应
   - ✅ 按钮大小合适
   - ✅ 文字清晰可读

## 🔍 调试技巧

### 查看导入的钱包数据

```javascript
// 在浏览器控制台执行
const walletData = localStorage.getItem('multichain_wallet_data');
if (walletData) {
  const parsed = JSON.parse(walletData);
  console.log('钱包名称:', parsed.wallet.name);
  console.log('钱包地址:', parsed.wallet.addresses);
  console.log('创建时间:', new Date(parsed.wallet.createdAt));
} else {
  console.log('未找到钱包数据');
}
```

### 验证助记词

```javascript
// 在浏览器控制台执行
import * as bip39 from 'bip39';

const mnemonic = 'abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about';
console.log('助记词有效:', bip39.validateMnemonic(mnemonic));
```

### 清除钱包数据

```javascript
// 清除所有钱包数据（用于重新测试）
localStorage.removeItem('multichain_wallet_data');
sessionStorage.removeItem('multichain_wallet_session');
console.log('钱包数据已清除');
location.reload();
```

## ✅ 测试检查清单

### 功能测试
- [ ] 助记词导入功能正常
- [ ] 私钥导入界面显示正常
- [ ] 链选择器工作正常
- [ ] 私钥格式验证正确
- [ ] 地址派生验证正常
- [ ] 密码强度指示器正常
- [ ] 错误提示清晰准确

### UI/UX 测试
- [ ] 模式切换流畅
- [ ] 按钮状态正确（启用/禁用）
- [ ] 加载状态显示
- [ ] 响应式设计正常
- [ ] 暗色主题显示正常
- [ ] 图标和文字清晰

### 安全测试
- [ ] 助记词验证正确
- [ ] 私钥格式验证正确
- [ ] 密码强度验证正确
- [ ] 安全警告显示
- [ ] 加密存储正常

### 集成测试
- [ ] 路由配置正确
- [ ] 多个入口点可用
- [ ] 导入后跳转正常
- [ ] 钱包数据保存正确
- [ ] 可以正常解锁

## 🐛 常见问题

### 问题1：助记词导入失败

**可能原因：**
- 助记词格式不正确
- 单词拼写错误
- 单词数量不对（不是12或24个）

**解决方案：**
1. 检查助记词格式
2. 确保单词之间用空格分隔
3. 使用标准 BIP39 单词列表

### 问题2：私钥导入显示限制

**说明：**
- 私钥导入功能目前处于测试阶段
- 仅支持单链钱包
- 建议使用助记词导入

**解决方案：**
- 使用助记词导入以获得完整功能

### 问题3：密码强度不足

**解决方案：**
1. 使用至少8个字符
2. 包含大小写字母
3. 包含数字
4. 包含特殊字符
5. 示例：`MyWallet@2024`

### 问题4：导入后看不到地址

**解决方案：**
1. 刷新页面
2. 检查是否已解锁
3. 查看浏览器控制台错误
4. 清除缓存重试

## 📊 性能指标

预期性能：
- 助记词验证：< 100ms
- 地址派生：< 2s（4条链）
- 私钥验证：< 500ms
- 加密存储：< 500ms

## 🎯 测试目标

- ✅ 功能完整性：所有功能都能正常工作
- ✅ 用户体验：流程顺畅，提示清晰
- ✅ 安全性：验证正确，加密存储
- ✅ 错误处理：各种错误情况都有提示
- ✅ 性能：响应速度快

## 📝 测试报告模板

```
测试日期：____________________
测试人员：____________________
浏览器：____________________

测试结果：
□ 通过  □ 失败

功能测试：
□ 助记词导入  □ 私钥导入  □ 错误处理

问题描述：
_________________________________
_________________________________

建议改进：
_________________________________
_________________________________
```

## 🎓 测试技巧

### 1. 使用测试助记词

标准测试助记词（BIP39）：
```
abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about
```

这个助记词会派生出固定的地址，方便测试。

### 2. 使用浏览器开发者工具

- F12 打开开发者工具
- 查看控制台错误
- 检查网络请求
- 查看 localStorage 数据

### 3. 测试不同场景

- 正常流程
- 错误输入
- 边界情况
- 网络异常

### 4. 记录测试结果

- 截图保存
- 记录错误信息
- 记录性能数据

---

**祝测试顺利！** 🎉

如有问题，请查看：
- 开发者工具控制台
- `PHASE_5_STEP_9_WALLET_IMPORT_COMPLETE.md`
- `PHASE_5_STEP_8_WALLET_CREATION_VERIFICATION.md`
