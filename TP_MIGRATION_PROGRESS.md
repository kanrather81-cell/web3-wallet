# TP 钱包风格迁移进度

## 迁移策略
采用渐进式迁移，每次只迁移一个页面，确保所有功能完整。

## 迁移原则
1. 保持所有原有功能完整
2. 使用 TP 钱包的浅色风格（bg-gray-50）
3. 统一使用 BottomNavTP 底部导航
4. 顶部使用渐变背景（bg-gradient-tp）
5. 卡片使用圆角（rounded-2xl）和阴影
6. 主色调使用 primary-600 (#2563eb)

## 已完成迁移

### ✅ Phase 1: 基础页面
- [x] **AssetHomeTP** - 资产首页（简化版）
- [x] **DiscoverTP** - 发现页面
- [x] **ProfileTP** - 个人中心
- [x] **MarketPage** - 行情页面 ✨
- [x] **SwapPage** - 兑换页面 ✨
- [x] **SettingsPage** - 设置页面 ✨
- [x] **MyDAppsPage** - 我的 DApps ✨

### ✅ Phase 2: 功能页面（中等复杂度）
- [x] **TransactionHistoryPage** - 交易历史 ✨
- [x] **BrowserPage** - 浏览器 ✨

### ✅ Phase 3: 复杂页面（已完成）
- [x] **SendPage** - 发送页面 ✨
- [x] **ReceivePage** - 接收页面 ✨
- [x] **TokenDetailPage** - 代币详情 ✨
- [x] **CoinDetailPage** - 币种详情 ✨
- [x] **TxDetailsPage** - 交易详情 ✨

### ✅ Phase 4: 特殊页面（已完成）
- [x] **AssetsPage** - 资产完整版 ✨
- [x] **WalletsPage** - 钱包管理 ✨

#### SendPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部添加 TP 风格渐变区域 `bg-gradient-tp`
3. 表单卡片改为白色圆角设计 `rounded-2xl`
4. 输入框使用浅灰色背景 `bg-gray-50`
5. 链选择按钮改为浅色风格
6. 状态提示改为浅色（绿色、黄色、红色）
7. 所有文字改为深色（text-gray-900, text-gray-600）
8. 按钮使用 primary-600 渐变
9. 移除了返回按钮（使用底部导航）

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/send`, `/send/:chainId/:tokenAddress`

**保留的功能**:
- ✅ 多链支持（Ethereum, Solana, Bitcoin, Tron）
- ✅ 链选择切换
- ✅ 钱包连接状态检测
- ✅ 接收地址输入
- ✅ 金额输入
- ✅ Gas 费用选择器
- ✅ 高级选项（代币合约地址）
- ✅ 交易确认对话框
- ✅ 错误和成功提示
- ✅ 交易历史记录保存
- ✅ 跳转到交易详情页

#### ReceivePage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部改为 TP 风格渐变区域 `bg-gradient-tp`
3. QR 码容器改为白色圆角卡片
4. 地址显示卡片改为白色背景
5. 复制按钮使用 primary-600 蓝色
6. 下载按钮改为白色边框样式
7. 警告提示改为黄色浅色背景
8. 所有文字改为深色（text-gray-900, text-gray-600）

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/receive/:chainId`, `/receive/:chainId/:tokenAddress`

**保留的功能**:
- ✅ QR 码生成和显示
- ✅ 钱包地址显示
- ✅ 复制地址功能
- ✅ 下载 QR 码功能
- ✅ 多链支持
- ✅ 安全警告提示
- ✅ 加载状态处理

#### TokenDetailPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部改为 TP 风格渐变区域 `bg-gradient-tp`
3. 代币图标使用白色半透明圆形背景
4. 余额显示卡片改为白色半透明背景
5. 操作按钮（发送/接收）改为白色半透明背景
6. 代币信息卡片改为白色圆角设计
7. 所有文字改为深色（text-gray-900, text-gray-600）
8. 复制地址按钮使用浅色风格

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/token/:chainId`, `/token/:chainId/:tokenAddress`

**保留的功能**:
- ✅ 代币余额显示
- ✅ 多链支持
- ✅ 发送/接收快捷入口
- ✅ 钱包地址复制
- ✅ 跳转到交易历史
- ✅ 加载状态处理

#### CoinDetailPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部改为 TP 风格渐变区域 `bg-gradient-tp`
3. 币种图标和名称显示在顶部渐变区域
4. 价格显示卡片改为白色半透明背景
5. 图表卡片改为白色圆角设计
6. 时间范围按钮改为浅色风格（激活状态使用 primary-600）
7. 统计数据卡片改为白色圆角设计
8. 描述卡片改为白色圆角设计
9. 所有文字改为深色（text-gray-900, text-gray-600）

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/market/:coinId`

**保留的功能**:
- ✅ 实时价格显示
- ✅ 24小时涨跌幅
- ✅ 价格走势图表（ECharts）
- ✅ 多时间范围切换（24H, 7D, 30D, 90D, 1Y）
- ✅ 市值、交易量、流通供应量统计
- ✅ 币种描述信息
- ✅ 加载状态处理

#### TxDetailsPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部改为 TP 风格渐变区域 `bg-gradient-tp`
3. 链信息卡片改为白色圆角设计
4. 交易状态组件使用浅色风格
5. 交易哈希卡片改为白色圆角设计
6. 交易详情卡片改为白色圆角设计
7. 提示信息改为蓝色浅色背景
8. 操作按钮改为浅色风格
9. 所有文字改为深色（text-gray-900, text-gray-600）

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/tx/:txHash`

**保留的功能**:
- ✅ 交易状态实时显示
- ✅ 自动刷新状态
- ✅ 确认数显示
- ✅ 区块浏览器链接
- ✅ 发送/接收地址显示
- ✅ 转账金额显示
- ✅ 代币合约地址显示
- ✅ Gas 费用显示
- ✅ 交易时间显示
- ✅ 跳转到历史记录和发送新交易

#### AssetsPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部改为 TP 风格渐变区域 `bg-gradient-tp`
3. 总余额卡片改为白色半透明背景
4. 快捷操作按钮改为白色半透明背景（4个按钮：发送、历史、浏览器、刷新）
5. 资产分布图表卡片改为白色圆角设计
6. 链余额卡片改为白色圆角设计
7. Tabs 改为白色背景，激活状态使用 primary-600
8. 错误提示改为黄色浅色背景
9. 底部提示改为蓝色浅色背景
10. 所有文字改为深色（text-gray-900, text-gray-600）
11. 移除了顶部的多个导航按钮，简化为4个快捷操作

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/assets`

**保留的功能**:
- ✅ 钱包连接状态检测
- ✅ 多链余额聚合显示
- ✅ 总余额计算（ETH）
- ✅ 资产分布图表
- ✅ 多链连接器
- ✅ 链资产展示
- ✅ 4个 Tabs（代币、自定义、NFT、历史）
- ✅ 链余额列表
- ✅ 自定义代币列表
- ✅ NFT 画廊
- ✅ 交易历史
- ✅ 加载状态和错误处理
- ✅ 刷新功能

#### WalletsPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部改为 TP 风格渐变区域 `bg-gradient-tp`
3. 钱包卡片改为白色圆角设计
4. 激活钱包使用 primary-500 边框高亮
5. 空状态卡片改为白色圆角设计
6. 操作按钮改为浅色风格（切换、重命名、备份、删除）
7. 对话框改为白色圆角设计
8. 输入框使用浅灰色背景
9. 助记词网格使用白色卡片
10. 所有文字改为深色（text-gray-900, text-gray-600）
11. 移除了返回按钮（使用底部导航）

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/wallets`

**保留的功能**:
- ✅ 钱包列表显示
- ✅ 激活钱包标识
- ✅ 多链地址显示（ETH, SOL, BTC, TRX）
- ✅ 钱包切换功能
- ✅ 钱包重命名
- ✅ 钱包备份（导出助记词）
- ✅ 钱包删除（需要密码确认）
- ✅ 创建新钱包入口
- ✅ 导入钱包入口
- ✅ 空状态提示
- ✅ 密码验证
- ✅ 助记词复制功能
- ✅ 错误处理和提示

#### SolanaTestPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部改为 TP 风格渐变区域 `bg-gradient-tp`
3. 钱包连接卡片改为白色圆角设计
4. 连接状态提示改为绿色浅色背景
5. 使用说明改为蓝色浅色背景
6. 所有文字改为深色（text-gray-900, text-gray-600）

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/test/solana`

**保留的功能**:
- ✅ Solana 钱包连接（Phantom、Solflare）
- ✅ SOL 余额显示
- ✅ 连接状态显示
- ✅ 使用说明

#### BitcoinTestPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部改为 TP 风格渐变区域 `bg-gradient-tp`
3. 钱包连接卡片改为白色圆角设计
4. 钱包信息卡片改为白色圆角设计
5. 地址显示使用浅灰色背景
6. 使用说明卡片改为白色圆角设计
7. 所有文字改为深色（text-gray-900, text-gray-600）

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/test/bitcoin`

**保留的功能**:
- ✅ Bitcoin 钱包连接（Unisat、Xverse、Leather）
- ✅ BTC 余额显示
- ✅ 地址显示
- ✅ 余额自动刷新（30秒）
- ✅ 手动刷新功能
- ✅ 断开连接功能
- ✅ 使用说明

#### TronTestPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部改为 TP 风格渐变区域 `bg-gradient-tp`
3. Tabs 改为白色背景，激活状态使用 primary-600
4. 所有卡片改为白色圆角设计
5. 地址显示使用浅灰色背景
6. TRC20 代币卡片改为浅灰色背景
7. 所有文字改为深色（text-gray-900, text-gray-600）

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/test/tron`

**保留的功能**:
- ✅ Tron 钱包连接（TronLink）
- ✅ TRX 余额显示
- ✅ TRC20 代币余额显示（USDT、USDC）
- ✅ 地址显示
- ✅ 余额自动刷新（30秒）
- ✅ 手动刷新功能
- ✅ 3个 Tabs（钱包连接、余额信息、TRC20代币）
- ✅ 使用说明

#### BrowserPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部导航栏改为 TP 风格渐变区域 `bg-gradient-tp`
3. 导航按钮改为半透明白色背景 `bg-white/20`
4. 地址栏改为白色背景 `bg-white/90`
5. 内容区域使用白色圆角卡片 `rounded-2xl`
6. 推荐 DApp 卡片改为浅色风格
7. 错误提示改为浅色风格
8. 安全警告改为黄色浅色背景
9. 所有文字改为深色（text-gray-900, text-gray-600）
10. 加载动画使用 primary-600 蓝色

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/browser`

**保留的功能**:
- ✅ 前进/后退/刷新导航
- ✅ 地址栏输入和搜索
- ✅ URL 验证和规范化
- ✅ 浏览历史管理
- ✅ HTTPS 安全指示器
- ✅ 钱包连接状态显示
- ✅ iframe 内容加载
- ✅ 推荐 DApp 快速访问（Uniswap, Aave, OpenSea）
- ✅ 错误处理和提示
- ✅ 安全警告（非 HTTPS 连接）
- ✅ 钱包提供者注入（简化版）

#### TransactionHistoryPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部添加 TP 风格渐变区域 `bg-gradient-tp`
3. 移除了返回按钮（使用底部导航）
4. 更新 MultiChainTransactionHistory 组件为浅色风格：
   - 筛选器卡片改为白色背景
   - 交易卡片改为白色圆角设计
   - 按钮使用 primary-600 蓝色
   - 所有文字改为深色（text-gray-900, text-gray-600）
   - 空状态和加载状态改为浅色

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/history`

**保留的功能**:
- ✅ 按链筛选（全部、Ethereum、Solana、Bitcoin、Tron）
- ✅ 按状态筛选（全部、进行中、成功、失败）
- ✅ 刷新交易记录
- ✅ 交易详情展示（链、状态、地址、时间、哈希、Gas 费用）
- ✅ 点击跳转到交易详情页
- ✅ 错误信息显示
- ✅ 统计信息显示

#### MyDAppsPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部添加 TP 风格渐变区域 `bg-gradient-tp`
3. DApp 卡片改为白色圆角设计
4. Tabs 改为白色背景，激活状态使用 primary-600
5. 空状态卡片改为浅色风格
6. 移除了返回按钮（使用底部导航）

**路由变更**:
- 保持在原路径：`/dapps`
- 从 ProfileTP 的"我的 DApps"入口访问（需要更新）

**保留的功能**:
- ✅ 收藏夹和最近访问两个标签
- ✅ DApp 卡片展示（图标、名称、分类、描述、链）
- ✅ 取消收藏功能
- ✅ 清除最近访问记录
- ✅ 点击跳转到浏览器
- ✅ 时间戳格式化显示
- ✅ 空状态提示

#### SettingsPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部添加 TP 风格渐变区域 `bg-gradient-tp`
3. 所有卡片改为白色圆角设计 `rounded-2xl`
4. 网络列表改为浅色卡片布局
5. 对话框改为浅色风格
6. 开关按钮颜色从 indigo 改为 primary
7. 移除了返回按钮（使用底部导航）

**路由变更**:
- 保持在原路径：`/settings`
- 从 ProfileTP 的"通用设置"入口访问

**保留的功能**:
- ✅ 语言切换
- ✅ 钱包管理入口
- ✅ 生物识别认证开关
- ✅ 自动锁定设置
- ✅ 网络管理（添加/编辑/删除/设为默认）
- ✅ RPC URL 验证
- ✅ 自定义网络支持
- ✅ 区块浏览器链接

#### SwapPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部添加 TP 风格渐变区域 `bg-gradient-tp`
3. 卡片改为白色圆角设计 `rounded-2xl`
4. 输入框使用浅灰色背景 `bg-gray-50`
5. 按钮使用 primary-600 蓝色
6. 功能特点改为垂直卡片布局
7. 所有文字改为中文
8. 移除了返回按钮（使用底部导航）

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/swap`

**底部导航更新**:
- 将"发现"改为"兑换"
- 图标使用 `Repeat`
- 导航顺序：资产、行情、兑换、我的

**保留的功能**:
- ✅ 代币选择下拉框
- ✅ 金额输入
- ✅ 余额显示
- ✅ 兑换方向切换按钮
- ✅ 外部链接到 LI.FI Jumper
- ✅ 功能特点展示
- ✅ 占位提示信息

#### MarketPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 顶部添加 TP 风格渐变区域 `bg-gradient-tp`
3. 搜索框移到顶部渐变区域内
4. 表格改为卡片列表布局
5. 每个币种使用独立的白色圆角卡片
6. 价格提醒对话框改为浅色风格
7. 移除了排序功能（简化 UI）
8. 保留了所有核心功能：
   - 搜索币种
   - 查看价格和涨跌幅
   - 设置价格提醒
   - 点击跳转到详情页

**路由变更**:
- 从 `MainLayout` 移至 `TPLayout`
- 路径保持不变：`/market`

**底部导航更新**:
- 将"兑换"改为"行情"
- 图标从 `Repeat` 改为 `TrendingUp`

**保留的功能**:
- ✅ 实时行情数据加载
- ✅ 币种搜索
- ✅ 价格提醒创建/删除
- ✅ 通知权限请求
- ✅ 跳转到币种详情页

### ✅ Phase 5: 测试页面（已完成）
- [x] **SolanaTestPage** - Solana 测试页面 ✨
- [x] **BitcoinTestPage** - Bitcoin 测试页面 ✨
- [x] **TronTestPage** - Tron 测试页面 ✨

### ✅ Phase 6: 钱包设置页面（已完成）
- [x] **WalletSetupPage** - 钱包设置引导页 ✨ NEW
- [x] **CreateWalletPage** - 创建钱包页面 ✨ NEW
- [x] **UnlockWalletPage** - 解锁钱包页面 ✨ NEW
- [x] **ImportWalletPage** - 导入钱包页面 ✨ NEW

## 待迁移页面

无 - 所有页面已完成迁移！

## 迁移完成情况

🎉 所有页面（包括测试页面和钱包设置页面）已完成迁移！

## 当前系统状态

### TP 风格页面（TPLayout + BottomNavTP）
- `/` - AssetHomeTP
- `/discover` - DiscoverTP
- `/profile` - ProfileTP
- `/market` - MarketPage ✨
- `/swap` - SwapPage ✨
- `/history` - TransactionHistoryPage ✨
- `/browser` - BrowserPage ✨
- `/send` - SendPage ✨
- `/receive/:chainId` - ReceivePage ✨
- `/token/:chainId` - TokenDetailPage ✨
- `/market/:coinId` - CoinDetailPage ✨
- `/tx/:txHash` - TxDetailsPage ✨
- `/assets` - AssetsPage ✨
- `/wallets` - WalletsPage ✨
- `/test/solana` - SolanaTestPage ✨
- `/test/bitcoin` - BitcoinTestPage ✨
- `/test/tron` - TronTestPage ✨

### 独立页面（无底部导航）
- `/wallet-setup` - WalletSetupPage ✨ NEW
- `/create-wallet` - CreateWalletPage ✨ NEW
- `/unlock-wallet` - UnlockWalletPage ✨ NEW
- `/import-wallet` - ImportWalletPage ✨ NEW

### 传统风格页面（MainLayout + BottomNav）
- `/dapps` - MyDAppsPage（从 ProfileTP 访问）
- `/settings` - SettingsPage（从 ProfileTP 访问）

## 下一步计划

无 - 所有页面迁移已完成！

## 迁移完成总结

🎉 **所有页面已完成 TP 钱包风格迁移！**

已完成 23 个页面的迁移，覆盖了应用的所有功能：
- 资产管理（首页、完整版）
- 交易功能（发送、接收、历史）
- 市场行情（行情列表、币种详情）
- DApp 生态（发现、浏览器、我的 DApps）
- 用户中心（个人中心、设置、钱包管理）
- 详情页面（代币详情、交易详情）
- 测试页面（Solana、Bitcoin、Tron）
- 钱包设置（引导、创建、解锁、导入）

整个应用已实现完全统一的现代化浅色主题设计，包括所有测试页面和钱包设置流程。

## 迁移检查清单

每个页面迁移时需要确认：
- [ ] 背景改为 `bg-gray-50`
- [ ] 顶部使用 `bg-gradient-tp`
- [ ] 卡片使用 `rounded-2xl` 和白色背景
- [ ] 文字颜色改为深色（text-gray-900, text-gray-600）
- [ ] 按钮使用 primary-600 或其他明亮颜色
- [ ] 底部留出导航栏空间（pb-24）
- [ ] 所有原有功能正常工作
- [ ] 没有语法错误或警告
- [ ] 路由正确配置到 TPLayout

## 测试要点

每次迁移后需要测试：
1. 页面正常加载
2. 所有按钮和链接正常工作
3. 数据正确显示
4. 表单提交正常
5. 导航跳转正确
6. 移动端响应式正常
7. 没有控制台错误

## 备注

- 保留 `AssetsPage.tsx.backup` 作为参考
- 每次迁移前先备份原文件
- 遇到问题立即回滚
- 保持小步快跑，逐个页面迁移


#### WalletSetupPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 卡片改为白色圆角设计 `bg-white rounded-2xl`
3. 创建钱包按钮使用 primary-600 蓝色
4. 导入钱包按钮使用浅灰色 `bg-gray-200`
5. 安全提示改为黄色浅色背景
6. 所有文字改为深色（text-gray-900, text-gray-600）
7. 悬停效果改为阴影和边框高亮

**路由变更**:
- 保持独立路由：`/wallet-setup`
- 无底部导航（首次使用引导页）

**保留的功能**:
- ✅ 创建新钱包入口
- ✅ 导入现有钱包入口
- ✅ 使用外部钱包连接入口
- ✅ 安全提示展示
- ✅ 功能特点说明

#### CreateWalletPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 所有卡片改为白色圆角设计 `bg-white rounded-2xl`
3. 步骤指示器使用 primary-600 蓝色
4. 助记词网格使用白色卡片和浅灰色边框
5. 输入框使用浅灰色背景 `bg-gray-50`
6. 按钮使用 primary-600 蓝色
7. 安全警告改为红色/黄色浅色背景
8. 所有文字改为深色（text-gray-900, text-gray-600）
9. 密码强度指示器使用浅色背景

**路由变更**:
- 保持独立路由：`/create-wallet`
- 无底部导航（钱包创建流程）

**保留的功能**:
- ✅ 3步创建流程（生成、验证、密码）
- ✅ 助记词生成和显示
- ✅ 助记词复制功能
- ✅ 助记词验证（随机3个单词）
- ✅ 密码强度检测
- ✅ 钱包名称自定义
- ✅ 安全提示确认
- ✅ 创建成功提示

#### UnlockWalletPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 卡片改为白色圆角设计 `bg-white rounded-2xl`
3. 输入框使用浅灰色背景 `bg-gray-50`
4. 解锁按钮使用 primary-600 蓝色
5. 错误提示改为红色浅色背景
6. 安全提示改为黄色浅色背景
7. 所有文字改为深色（text-gray-900, text-gray-600）
8. 链接使用 primary-600 蓝色

**路由变更**:
- 保持独立路由：`/unlock-wallet`
- 无底部导航（解锁流程）

**保留的功能**:
- ✅ 密码输入和验证
- ✅ 活动钱包名称显示
- ✅ 切换到其他钱包入口
- ✅ 导入其他钱包入口
- ✅ 错误提示
- ✅ 安全提示

#### ImportWalletPage 迁移详情
**迁移时间**: 2024-02-26

**改动内容**:
1. 背景从深色渐变改为浅色 `bg-gray-50`
2. 所有卡片改为白色圆角设计 `bg-white rounded-2xl`
3. 导入方式切换按钮使用 primary-600 蓝色
4. 链选择按钮使用 primary-50 背景和 primary-500 边框
5. 输入框使用浅灰色背景 `bg-gray-50`
6. 导入按钮使用 primary-600 蓝色
7. 提示信息改为蓝色/黄色/红色浅色背景
8. 所有文字改为深色（text-gray-900, text-gray-600）
9. 密码强度指示器使用浅色背景

**路由变更**:
- 保持独立路由：`/import-wallet`
- 无底部导航（导入流程）

**保留的功能**:
- ✅ 助记词导入（12/24个单词）
- ✅ 私钥导入（多链支持）
- ✅ 链选择（ETH/SOL/BTC/TRX）
- ✅ 私钥格式验证
- ✅ 地址派生验证
- ✅ 密码强度检测
- ✅ 钱包名称自定义
- ✅ 安全提示确认
- ✅ 错误提示和警告
